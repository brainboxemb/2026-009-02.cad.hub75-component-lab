name: SCAD lab build

on:
  workflow_dispatch:
  pull_request:

permissions:
  contents: write
  packages: read

jobs:
  scad:
    uses: brainboxemb/tool.scad-project/.github/workflows/project-build.yml@a140b22858ac1899e7f2fa71b679639a70d819c3
    with:
      artifact_name: hub75-component-lab-build
      publish: false
