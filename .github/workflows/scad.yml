name: SCAD lab build

on:
  workflow_dispatch:
  pull_request:

permissions:
  contents: write
  packages: read

jobs:
  scad:
    uses: brainboxemb/tool.scad-project/.github/workflows/project-build.yml@229aea1475d3fda2dc9252e6e442708df44b8812
    with:
      artifact_name: hub75-component-lab-build
      publish: true
